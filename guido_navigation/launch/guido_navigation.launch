<?xml version="1.0" ?>

<launch>
  <!-- Arguments -->
  <arg name="map_file" default="my_map"/>
  <arg name="move_forward_only" default="true"/>
  <arg name="exploration" default="false"/>

  <!-- Map Publishing Node for Exploration-->
  <group if="$(arg exploration)">
    <node name="cartographer_occupancy_grid_node" pkg="cartographer_ros" type="cartographer_occupancy_grid_node" args="-resolution 0.01" />
  </group>

  <!-- Map server for Map-based Navigation-->
  <group unless="$(arg exploration)">
    <node pkg="map_server" name="map_server" type="map_server" args="$(find guido_navigation)/maps/$(arg map_file).yaml"/>
  </group>

  <!-- AMCL -->
  <!-- include file="$(find guido_navigation)/launch/amcl.launch"/ -->

  <!-- rviz -->
  <node pkg="rviz" type="rviz" name="rviz" required="true" args="-d $(find guido_navigation)/rviz/guido_navigation.rviz"/>

  <!-- move_base -->
  <include file="$(find guido_navigation)/launch/move_base.launch">
    <arg name="move_forward_only" value="$(arg move_forward_only)"/>
  </include>

</launch>